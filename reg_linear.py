def regressao_linear(x1, x2):
    # Coeficientes da regressão linear (exemplo)
    a = 0.5  # inclinação
    b = 2.0  # interceptação

    # Fórmula de regressão linear simples: y = a*x1 + b*x2
    previsao = a * x1 + b * x2
    return previsao
